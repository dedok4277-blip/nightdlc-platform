# 🗄️ Настройка двух баз данных

## Быстрый старт

### 1. Настройте .env файл

```env
# PostgreSQL от Render (облако)
DATABASE_URL=postgresql://user:pass@host.render.com/db

# MySQL от XAMPP (локально)
XAMPP_ENABLED=true
XAMPP_HOST=localhost
XAMPP_USER=root
XAMPP_PASSWORD=
XAMPP_DB_NAME=nelondlc
XAMPP_PORT=3306

# Какая БД главная? (postgres или mysql)
PRIMARY_DB=postgres

# Копировать данные во вторую БД? (true/false)
SYNC_DATABASES=true
```

### 2. Измените server/index.js

Найдите строку:
```javascript
import pool, { nextUid } from './db-adapter.js'
```

Замените на:
```javascript
import pool, { nextUid } from './db-dual.js'
```

### 3. Создайте БД в XAMPP

1. Откройте XAMPP → запустите MySQL
2. Откройте http://localhost/phpmyadmin
3. Создайте базу `nelondlc`

### 4. Запустите сервер

```bash
npm start
```

Вы увидите:
```
✅ PostgreSQL (Render) connected
✅ MySQL (XAMPP) connected
📊 Primary database: postgres
```

## 🧪 Проверка

```bash
node test-dual-db.js
```

## 🔄 Ручная синхронизация

```bash
node sync-dual-databases.js
```

## 📊 Режимы работы

### Render главная, XAMPP бэкап (рекомендуется)
```env
PRIMARY_DB=postgres
SYNC_DATABASES=true
```

### XAMPP главная, Render бэкап
```env
PRIMARY_DB=mysql
SYNC_DATABASES=true
```

### Только Render
```env
PRIMARY_DB=postgres
XAMPP_ENABLED=false
```

### Только XAMPP
```env
PRIMARY_DB=mysql
DATABASE_URL=
```

## ⚠️ Важно

- Данные копируются только из главной БД во вторую
- Не редактируйте вторую БД напрямую
- Используйте вторую БД для чтения и бэкапов

## 📖 Подробная инструкция

Смотрите `DUAL-DATABASE-SETUP.md`
