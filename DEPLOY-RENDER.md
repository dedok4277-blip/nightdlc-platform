# 🚀 Деплой NelonDLC на Render.com

Полная инструкция по развертыванию вашего сайта на Render.com с MySQL базой данных.

---

## 📋 Что нужно подготовить

1. Аккаунт на [Render.com](https://render.com) (можно зарегистрироваться через GitHub)
2. Ваш проект загружен на GitHub
3. 10-15 минут времени

---

## 🗄️ Шаг 1: Создание MySQL базы данных

### 1.1 Создайте новую базу данных

1. Войдите в [Dashboard Render.com](https://dashboard.render.com/)
2. Нажмите **"New +"** → **"MySQL"**
3. Заполните форму:
   - **Name**: `nelondlc-db` (или любое имя)
   - **Database**: `nelondlc`
   - **User**: `nelondlc_user` (или оставьте по умолчанию)
   - **Region**: выберите ближайший к вам регион (например, Frankfurt для Европы)
   - **Plan**: выберите **Free** (или платный, если нужно)

4. Нажмите **"Create Database"**

### 1.2 Сохраните данные подключения

После создания базы данных вы увидите:
- **Internal Database URL** - используйте этот URL для подключения
- **Hostname**
- **Port**
- **Database**
- **Username**
- **Password**

⚠️ **Важно**: Сохраните эти данные, они понадобятся на следующем шаге!

---

## 🌐 Шаг 2: Создание Web Service

### 2.1 Создайте новый сервис

1. В Dashboard нажмите **"New +"** → **"Web Service"**
2. Подключите ваш GitHub репозиторий:
   - Нажмите **"Connect account"** если еще не подключили GitHub
   - Выберите репозиторий с проектом NelonDLC
   - Нажмите **"Connect"**

### 2.2 Настройте сервис

Заполните форму:

- **Name**: `nelondlc` (или любое имя, это будет в URL)
- **Region**: тот же регион, что и база данных
- **Branch**: `main` (или ваша основная ветка)
- **Root Directory**: оставьте пустым
- **Runtime**: **Node**
- **Build Command**: 
  ```bash
  npm install && npm run build
  ```
- **Start Command**:
  ```bash
  npm start
  ```
- **Plan**: выберите **Free** (или платный)

### 2.3 Добавьте переменные окружения

Прокрутите вниз до раздела **"Environment Variables"** и добавьте:

```
NODE_ENV=production
PORT=10000
JWT_SECRET=ваш-секретный-ключ-минимум-32-символа
DB_HOST=хост-из-render-mysql
DB_USER=пользователь-из-render-mysql
DB_PASSWORD=пароль-из-render-mysql
DB_NAME=nelondlc
DB_PORT=3306
```

**Где взять данные для DB_***:
- Откройте вашу MySQL базу в Render Dashboard
- Скопируйте **Hostname** → вставьте в `DB_HOST`
- Скопируйте **Username** → вставьте в `DB_USER`
- Скопируйте **Password** → вставьте в `DB_PASSWORD`
- Скопируйте **Database** → вставьте в `DB_NAME`

**Для JWT_SECRET**:
- Сгенерируйте случайную строку минимум 32 символа
- Можно использовать: `node generate-key.js` или онлайн генератор

### 2.4 Запустите деплой

1. Нажмите **"Create Web Service"**
2. Render начнет автоматический деплой (займет 3-5 минут)
3. Следите за логами в реальном времени

---

## 🗃️ Шаг 3: Инициализация базы данных

После успешного деплоя нужно создать таблицы в MySQL.

### 3.1 Подключитесь к базе через Shell

1. Откройте вашу MySQL базу в Render Dashboard
2. Перейдите на вкладку **"Shell"**
3. Нажмите **"Connect"**

### 3.2 Создайте таблицы

Выполните следующие SQL команды:

```sql
-- Таблица пользователей
CREATE TABLE IF NOT EXISTS users (
  id INT AUTO_INCREMENT PRIMARY KEY,
  uid INT NOT NULL UNIQUE,
  username VARCHAR(255) NOT NULL UNIQUE,
  email VARCHAR(255) NOT NULL UNIQUE,
  password_hash VARCHAR(255) NOT NULL,
  avatar_url VARCHAR(500),
  is_admin TINYINT(1) DEFAULT 0,
  license_key VARCHAR(255),
  created_at BIGINT NOT NULL,
  last_login BIGINT,
  plan VARCHAR(50) DEFAULT 'Nelon',
  status VARCHAR(50) DEFAULT 'Active',
  subscription_tier VARCHAR(50) DEFAULT 'None',
  subscription_expires_at BIGINT DEFAULT 0,
  hwid VARCHAR(255),
  INDEX idx_username (username),
  INDEX idx_email (email),
  INDEX idx_uid (uid)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- Таблица постов
CREATE TABLE IF NOT EXISTS posts (
  id INT AUTO_INCREMENT PRIMARY KEY,
  user_id INT NOT NULL,
  server VARCHAR(50) NOT NULL,
  title VARCHAR(255) NOT NULL,
  description TEXT,
  screenshot_path VARCHAR(500),
  download_url VARCHAR(500),
  view_count INT DEFAULT 0,
  created_at BIGINT NOT NULL,
  FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE,
  INDEX idx_user_id (user_id),
  INDEX idx_created_at (created_at)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- Таблица лайков
CREATE TABLE IF NOT EXISTS post_likes (
  id INT AUTO_INCREMENT PRIMARY KEY,
  user_id INT NOT NULL,
  post_id INT NOT NULL,
  created_at BIGINT NOT NULL,
  UNIQUE KEY unique_like (user_id, post_id),
  FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE,
  FOREIGN KEY (post_id) REFERENCES posts(id) ON DELETE CASCADE,
  INDEX idx_post_id (post_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- Таблица лицензионных ключей
CREATE TABLE IF NOT EXISTS license_keys (
  id INT AUTO_INCREMENT PRIMARY KEY,
  `key` VARCHAR(255) NOT NULL UNIQUE,
  used TINYINT(1) DEFAULT 0,
  created_at BIGINT NOT NULL,
  created_by INT,
  used_at BIGINT,
  used_by INT,
  FOREIGN KEY (created_by) REFERENCES users(id) ON DELETE SET NULL,
  FOREIGN KEY (used_by) REFERENCES users(id) ON DELETE SET NULL,
  INDEX idx_key (`key`),
  INDEX idx_used (used)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

### 3.3 Проверьте таблицы

```sql
SHOW TABLES;
```

Вы должны увидеть 4 таблицы: `users`, `posts`, `post_likes`, `license_keys`

---

## ✅ Шаг 4: Проверка работы

### 4.1 Откройте ваш сайт

1. В Render Dashboard откройте ваш Web Service
2. Вверху страницы найдите URL вида: `https://nelondlc.onrender.com`
3. Откройте его в браузере

### 4.2 Первый вход

При первом запуске автоматически создается админ:
- **Логин**: `admin`
- **Пароль**: `admin`

⚠️ **Важно**: Сразу после входа смените пароль в настройках!

### 4.3 Проверьте функционал

- ✅ Регистрация нового пользователя
- ✅ Вход в систему
- ✅ Создание поста на форуме
- ✅ Админ панель (если вошли как admin)

---

## 🔧 Дополнительные настройки

### Автоматический деплой

Render автоматически деплоит при каждом push в GitHub:
1. Сделайте изменения в коде
2. Закоммитьте и запушьте в GitHub
3. Render автоматически пересоберет и задеплоит

### Просмотр логов

1. Откройте ваш Web Service в Render Dashboard
2. Перейдите на вкладку **"Logs"**
3. Здесь вы увидите все логи приложения в реальном времени

### Настройка домена

Если хотите использовать свой домен:
1. В настройках Web Service найдите **"Custom Domain"**
2. Добавьте ваш домен
3. Настройте DNS записи согласно инструкциям Render

---

## 🐛 Решение проблем

### Ошибка подключения к базе данных

**Проблема**: `ECONNREFUSED` или `Access denied`

**Решение**:
1. Проверьте переменные окружения `DB_HOST`, `DB_USER`, `DB_PASSWORD`
2. Убедитесь, что используете **Internal Database URL** из Render
3. Проверьте, что база данных запущена (зеленый статус в Dashboard)

### Сайт не открывается

**Проблема**: 404 или белый экран

**Решение**:
1. Проверьте логи в Render Dashboard
2. Убедитесь, что Build Command выполнился успешно
3. Проверьте, что папка `dist` создалась после билда

### Долгая загрузка (cold start)

**Проблема**: Первый запрос занимает 30-60 секунд

**Решение**:
- Это нормально для Free плана Render
- Сервис "засыпает" после 15 минут неактивности
- Первый запрос "будит" его
- Решение: перейти на платный план ($7/месяц)

---

## 💰 Стоимость

### Free план (бесплатно)
- ✅ 750 часов в месяц
- ✅ Автоматический SSL
- ✅ Автодеплой из GitHub
- ⚠️ Сервис засыпает после 15 минут неактивности
- ⚠️ Ограничения по ресурсам

### Starter план ($7/месяц)
- ✅ Всегда активен (no cold starts)
- ✅ Больше ресурсов
- ✅ Приоритетная поддержка

---

## 📚 Полезные ссылки

- [Документация Render](https://render.com/docs)
- [Render Community](https://community.render.com/)
- [Статус Render](https://status.render.com/)

---

## 🎉 Готово!

Ваш сайт NelonDLC теперь доступен онлайн 24/7!

**Ваш URL**: `https://ваше-имя.onrender.com`

Если возникли проблемы - проверьте логи в Render Dashboard или создайте issue в GitHub репозитории.
