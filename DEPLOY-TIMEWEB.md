# Инструкция по загрузке на Timeweb

## ✅ Что уже готово:
- Проект собран (`npm run build` выполнен)
- База данных настроена в `.env`
- MySQL версия приложения готова

## 📤 Шаг 1: Загрузка файлов через FTP

### Подключитесь к FTP:
- **Хост**: `cz505339.tw1.ru`
- **Пользователь**: `cz505339`
- **Пароль**: ваш пароль от Timeweb
- **Порт**: 21

### Загрузите эти файлы и папки:
```
✅ dist/                    (собранный фронтенд)
✅ server/                  (бэкенд)
✅ uploads/                 (папка для загрузок)
✅ package.json
✅ package-lock.json
✅ .env                     (с настройками базы данных)
✅ ecosystem.config.js
✅ vite.config.js

❌ НЕ загружайте:
- node_modules/
- src/
- .git/
```

## 🔧 Шаг 2: Подключение по SSH

Откройте терминал (PowerShell или PuTTY) и подключитесь:

```bash
ssh cz505339@cz505339.tw1.ru
```

Введите пароль от Timeweb.

## 📦 Шаг 3: Установка зависимостей

```bash
# Перейдите в папку сайта
cd ~/cz505339.tw1.ru/public_html

# Установите зависимости
npm install --production
```

## 🚀 Шаг 4: Запуск приложения

```bash
# Установите PM2 (если еще не установлен)
npm install -g pm2

# Запустите приложение
pm2 start ecosystem.config.js

# Сохраните конфигурацию
pm2 save

# Настройте автозапуск
pm2 startup
```

## ✅ Шаг 5: Проверка

```bash
# Проверьте статус
pm2 status

# Посмотрите логи
pm2 logs nelondlc
```

Откройте в браузере: `http://cz505339.tw1.ru`

## 🔐 Первый вход

После запуска автоматически создастся администратор:
- **Логин**: `admin`
- **Пароль**: `admin`

⚠️ **Обязательно смените пароль после первого входа!**

## 🔄 Обновление приложения

Когда нужно обновить код:

```bash
# На локальном компьютере
npm run build

# Загрузите новые файлы через FTP

# На сервере
pm2 restart nelondlc
```

## 🐛 Troubleshooting

### Приложение не запускается
```bash
pm2 logs nelondlc --lines 50
```

### Ошибка подключения к базе данных
Проверьте `.env` на сервере:
```bash
cat .env
```

Должно быть:
```
DB_HOST=localhost
DB_USER=cz505339_nelondlc
DB_PASSWORD=pjmjMyAdmin
DB_NAME=cz505339_nelondlc
```

### Порт занят
```bash
# Проверьте, какой процесс использует порт
netstat -tulpn | grep 5173

# Остановите старый процесс
pm2 delete nelondlc
pm2 start ecosystem.config.js
```

## 📞 Нужна помощь?

Если что-то не работает, проверьте логи:
```bash
pm2 logs nelondlc --lines 100
```
