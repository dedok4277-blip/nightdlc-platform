# 🚀 Запуск MySQL для работы сервера

## Проблема

Сервер не может подключиться к MySQL:
```
Error: connect ECONNREFUSED 127.0.0.1:3306
```

## Решение

### Шаг 1: Запустите XAMPP

1. Откройте **XAMPP Control Panel**
2. Найдите строку **MySQL**
3. Нажмите кнопку **Start**

MySQL должен стать **зеленым**.

### Шаг 2: Если MySQL не запускается

#### Проблема: Порт 3306 занят

**Решение A: Остановите другой MySQL**

Откройте **Командную строку от администратора**:

```cmd
net stop MySQL80
net stop MySQL
```

Затем запустите MySQL в XAMPP.

**Решение B: Найдите и завершите процесс**

1. Откройте **Диспетчер задач** (Ctrl+Shift+Esc)
2. Вкладка **Службы**
3. Найдите **MySQL** или **MySQL80**
4. ПКМ -> **Остановить**

Затем запустите MySQL в XAMPP.

**Решение C: Проверьте порт**

```cmd
netstat -ano | findstr :3306
```

Если видите процесс - завершите его через диспетчер задач.

### Шаг 3: Запустите от администратора

Если MySQL все еще не запускается:

1. Закройте XAMPP
2. ПКМ на **XAMPP Control Panel**
3. **Запуск от имени администратора**
4. Нажмите **Start** рядом с MySQL

### Шаг 4: Проверьте подключение

```bash
cd "Site For NightDLC"
node test-mysql-connection.js
```

Должно показать:
```
✅ MySQL connection successful!
```

### Шаг 5: Запустите сервер

```bash
npm start
```

Должно показать:
```
✅ MySQL tables initialized
✅ Admin user created: admin / admin
✅ NelonDLC API listening on http://localhost:5173
📊 Database: MySQL (cz505339_nelondlc)
```

## Альтернатива: Используйте SQLite

Если MySQL не запускается, используйте SQLite (не требует XAMPP):

### Измените server/index.js

Найдите строку:
```javascript
import pool, { nextUid } from './db-mysql.js'
```

Замените на:
```javascript
import db from './db-sqlite-backup.js'
```

### Перезапустите сервер

```bash
npm start
```

SQLite работает сразу, без настройки!

## Проверка статуса MySQL

### В XAMPP

MySQL должен быть **зеленым** с портом **3306**.

### Через командную строку

```cmd
netstat -ano | findstr :3306
```

Должно показать:
```
TCP    0.0.0.0:3306    LISTENING    [PID]
```

### Через службы Windows

1. Win+R -> `services.msc`
2. Найдите **MySQL** или **MySQL80**
3. Статус должен быть **Работает**

## Частые проблемы

### MySQL запускается и сразу останавливается

**Причина:** Порт занят или поврежденные файлы

**Решение:**
1. Остановите все MySQL службы
2. Запустите XAMPP от администратора
3. Проверьте логи: XAMPP -> Logs -> MySQL Error Log

### Ошибка: Access denied

**Причина:** Неверные учетные данные

**Решение:** Проверьте `.env`:
```env
DB_USER=root
DB_PASSWORD=
```

Для XAMPP по умолчанию:
- User: `root`
- Password: пустой

### Ошибка: Unknown database

**Причина:** База данных не создана

**Решение:**
```bash
node create-database.js
```

Или создайте вручную через phpMyAdmin:
1. Откройте http://localhost/phpmyadmin
2. Создайте базу `cz505339_nelondlc`

## Быстрая помощь

```cmd
# 1. Остановите все MySQL
net stop MySQL80
net stop MySQL

# 2. Запустите XAMPP от админа
# ПКМ -> Запуск от имени администратора

# 3. Запустите MySQL в XAMPP

# 4. Проверьте подключение
cd "Site For NightDLC"
node test-mysql-connection.js

# 5. Запустите сервер
npm start
```

## Рекомендация

Для **локальной разработки**:
- Используйте **SQLite** (проще, не требует XAMPP)

Для **production** (Timeweb):
- Используйте **MySQL** на хостинге

---

**Дата**: Февраль 2026

Удачи! 🚀
