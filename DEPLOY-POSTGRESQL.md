# Деплой на Render с PostgreSQL

## 📋 Что нужно сделать

### 1. Получите строку подключения к базе данных

На странице вашей базы данных **nelondlc-db** в Render:

1. Перейдите на вкладку **Info**
2. Найдите **Internal Database URL**
3. Скопируйте полную строку (она начинается с `postgresql://`)

**Важно:** Используйте **Internal Database URL**, а не External!

### 2. Настройте Web Service на Render

1. Откройте ваш Web Service
2. Перейдите в **Environment**
3. Добавьте следующие переменные:

```bash
# Обязательные переменные
DATABASE_URL=postgresql://user:pass@dpg-xxxxx.oregon-postgres.render.com/nelondlc_db
NODE_ENV=production
JWT_SECRET=ваш-секретный-ключ-минимум-32-символа

# Опциональные
PORT=10000
```

**Генерация JWT_SECRET:**
```bash
node generate-key.js
```

### 3. Настройте Build & Deploy

В настройках Web Service:

**Build Command:**
```bash
npm run render-build
```

**Start Command:**
```bash
npm run render-start
```

### 4. Деплой

После сохранения переменных окружения Render автоматически запустит деплой.

## ✅ Проверка

### В логах должно появиться:

```
✅ Using PostgreSQL database
✅ PostgreSQL tables initialized
✅ Admin user created: admin / admin
✅ NelonDLC API listening on http://0.0.0.0:10000
```

### Тестирование API:

```bash
# Проверка здоровья
curl https://ваш-домен.onrender.com/api/health

# Должен вернуть:
{"ok":true}
```

### Вход в админ панель:

1. Откройте ваш сайт
2. Войдите с данными:
   - Логин: `admin`
   - Пароль: `admin`
3. **Сразу смените пароль!**

## 🔧 Локальная разработка

Для локальной разработки:

1. **НЕ добавляйте** `DATABASE_URL` в `.env`
2. Приложение автоматически будет использовать SQLite
3. Запустите: `npm run api`

## 🐛 Решение проблем

### Ошибка подключения к БД

```bash
# Проверьте подключение локально
npm run test:postgres
```

### База данных пустая

Таблицы создаются автоматически при первом запуске. Если их нет:
1. Проверьте логи на наличие ошибок
2. Убедитесь, что `DATABASE_URL` правильный
3. Проверьте, что база данных доступна

### Нет админа

Админ создается автоматически. Если его нет:
1. Проверьте логи
2. Попробуйте зарегистрировать нового пользователя
3. Выдайте ему права админа через скрипт:

```bash
node give-admin.js username
```

## 📊 Мониторинг

### Просмотр пользователей:

```bash
# Создайте скрипт show-users-postgres.js
node show-users-postgres.js
```

### Просмотр таблиц в БД:

1. Откройте базу данных на Render
2. Используйте встроенный SQL редактор
3. Или подключитесь через psql:

```bash
psql "postgresql://user:pass@host:5432/database"
```

## 🚀 Готово!

Ваше приложение теперь работает с PostgreSQL на Render!

Следующие шаги:
1. Смените пароль админа
2. Настройте домен (если нужно)
3. Добавьте пользователей
4. Создайте лицензионные ключи
