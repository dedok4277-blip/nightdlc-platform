# ✅ Чеклист деплоя с PostgreSQL на Render

## Перед деплоем

- [ ] База данных **nelondlc-db** создана на Render
- [ ] База данных в статусе **Available** (зеленый индикатор)
- [ ] Скопирован **Internal Database URL** из раздела Info

## Настройка Web Service

- [ ] Открыт Web Service на Render
- [ ] В разделе **Environment** добавлены переменные:
  - [ ] `DATABASE_URL` = Internal Database URL
  - [ ] `NODE_ENV` = production
  - [ ] `JWT_SECRET` = сгенерированный ключ (минимум 32 символа)
  - [ ] `PORT` = 10000 (опционально)

## Build настройки

- [ ] **Build Command:** `npm run render-build`
- [ ] **Start Command:** `npm run render-start`
- [ ] **Node Version:** 18 или выше

## После деплоя

- [ ] Деплой завершился успешно (зеленый статус)
- [ ] В логах есть сообщения:
  - [ ] `✅ Using PostgreSQL database`
  - [ ] `✅ PostgreSQL tables initialized`
  - [ ] `✅ Admin user created: admin / admin`
  - [ ] `✅ NelonDLC API listening on...`

## Тестирование

- [ ] API отвечает: `curl https://ваш-домен.onrender.com/api/health`
- [ ] Открывается главная страница сайта
- [ ] Можно войти с данными: admin / admin
- [ ] Админ панель открывается и показывает данные

## Безопасность

- [ ] Сменен пароль админа через интерфейс
- [ ] `JWT_SECRET` уникальный и сложный
- [ ] `.env` файл не закоммичен в Git
- [ ] Используется **Internal Database URL**, а не External

## Функциональность

- [ ] Регистрация новых пользователей работает
- [ ] Вход в систему работает
- [ ] Создание постов работает
- [ ] Загрузка аватаров работает
- [ ] Генерация лицензионных ключей работает
- [ ] Активация ключей работает

## Дополнительно (опционально)

- [ ] Настроен custom домен
- [ ] Настроены уведомления о деплое
- [ ] Настроен мониторинг
- [ ] Созданы бэкапы базы данных

## Полезные команды для проверки

```bash
# Проверка здоровья API
curl https://ваш-домен.onrender.com/api/health

# Проверка ping
curl https://ваш-домен.onrender.com/api/ping

# Просмотр пользователей (через Render Shell)
npm run users:postgres

# Тест подключения к БД (локально с DATABASE_URL в .env)
npm run test:postgres
```

## Если что-то пошло не так

### База данных не подключается
1. Проверьте статус БД на Render (должна быть Available)
2. Убедитесь, что используете **Internal** URL, а не External
3. Проверьте, что `DATABASE_URL` правильно скопирован

### Нет таблиц в БД
1. Проверьте логи на наличие ошибок инициализации
2. Таблицы создаются автоматически при первом запуске
3. Если ошибок нет, но таблиц нет - перезапустите сервис

### Не создался админ
1. Проверьте логи - должно быть "Admin user created"
2. Если нет, зарегистрируйте пользователя вручную
3. Выдайте права через Shell: `npm run admin:postgres username`

### Ошибки в логах
1. Проверьте все переменные окружения
2. Убедитесь, что `JWT_SECRET` установлен
3. Проверьте версию Node.js (должна быть 18+)

## Контакты и документация

- `DEPLOY-POSTGRESQL.md` - полная инструкция
- `ШПАРГАЛКА-POSTGRESQL.md` - краткая шпаргалка
- `POSTGRESQL-SCRIPTS.md` - описание скриптов
- `SETUP-POSTGRESQL.md` - детальная настройка

---

## ✅ Готово!

Если все пункты отмечены - ваше приложение успешно развернуто с PostgreSQL на Render!

**Следующие шаги:**
1. Смените пароль админа
2. Создайте тестового пользователя
3. Сгенерируйте лицензионные ключи
4. Поделитесь ссылкой с пользователями

**Удачи! 🚀**
