# ✅ Система лицензионных ключей - ГОТОВО

## Что реализовано

✅ **Выбор типа подписки при генерации ключа:**
- Basic - 30 дней
- Plus - 90 дней
- Lifetime - навсегда

✅ **Ограничение активации:**
- Каждый ключ можно активировать только **ОДИН РАЗ**
- После активации ключ блокируется навсегда
- Повторная активация невозможна

✅ **Автоматическое управление подписками:**
- Basic: автоматически истекает через 30 дней
- Plus: автоматически истекает через 90 дней
- Lifetime: никогда не истекает

## Файлы системы

### Основные скрипты

1. **generate-key-with-type.js** - Генерация одного ключа с выбором типа
2. **generate-multiple-keys.js** - Массовая генерация ключей (до 100 штук)
3. **show-keys-mysql.js** - Просмотр всех ключей и статистики
4. **test-keys-simple.js** - Тестирование системы

### Документация

1. **KEYS-SYSTEM-GUIDE.md** - Полное руководство (на английском)
2. **ИНСТРУКЦИЯ-КЛЮЧИ.md** - Краткая инструкция (на русском)
3. **СИСТЕМА-КЛЮЧЕЙ-ГОТОВО.md** - Этот файл

### Изменения в коде

1. **server/db-dual.js** - Добавлена колонка `subscription_type` в таблицу `license_keys`
2. **server/index.js** - Обновлены API endpoints:
   - `POST /api/admin/generate-key` - теперь принимает `subscriptionType`
   - `POST /api/me/activate-key` - проверяет тип ключа и выдает соответствующую подписку
   - `GET /api/admin/keys` - возвращает тип подписки для каждого ключа

## Быстрый старт

### 1. Сгенерировать ключ

```bash
node generate-key-with-type.js
```

Выберите тип:
- `1` - Basic (30 дней)
- `2` - Plus (90 дней)
- `3` - Lifetime (навсегда)

### 2. Посмотреть все ключи

```bash
node show-keys-mysql.js
```

### 3. Протестировать систему

```bash
node test-keys-simple.js
```

## Результаты тестирования

```
✅ Создан ключ Basic: 17F2-1127-2A2D
✅ Создан ключ Plus: 9C81-7BBE-F225
✅ Создан ключ Lifetime: DED2-C833-0030

🧪 Тестирование активации ключа Basic...
   ✅ Ключ валиден и не использован
   ✅ Ключ активирован!
   ✅ Подписка: Basic
   ✅ Истекает: 04.03.2026, 16:11:59
   ✅ Ключ помечен как использованный: Да

🧪 Попытка повторной активации...
   ✅ Повторная активация невозможна - ключ уже использован!
```

## API для админ-панели

### Генерация ключа

```javascript
POST /api/admin/generate-key
Authorization: Bearer <admin_token>

{
  "subscriptionType": "Lifetime"  // Basic, Plus или Lifetime
}

// Ответ:
{
  "key": "A1B2-C3D4-E5F6",
  "subscriptionType": "Lifetime"
}
```

### Просмотр всех ключей

```javascript
GET /api/admin/keys
Authorization: Bearer <admin_token>

// Ответ:
{
  "keys": [
    {
      "id": 1,
      "key": "A1B2-C3D4-E5F6",
      "subscriptionType": "Lifetime",
      "used": 0,
      "createdAt": 1738681919000,
      "usedAt": null,
      "createdBy": "admin",
      "usedBy": null
    }
  ]
}
```

### Активация ключа пользователем

```javascript
POST /api/me/activate-key
Authorization: Bearer <user_token>

{
  "key": "A1B2-C3D4-E5F6"
}

// Ответ при успехе:
{
  "user": {
    "uid": 1,
    "username": "user123",
    "subscriptionTier": "Lifetime",
    "subscriptionExpiresAt": 0,
    "subscriptionActive": true,
    "licenseKey": "A1B2-C3D4-E5F6"
  }
}

// Ошибки:
// 400 bad_request - ключ не указан
// 404 invalid_key - ключ не найден или уже использован
```

## Структура базы данных

```sql
CREATE TABLE license_keys (
  id INT AUTO_INCREMENT PRIMARY KEY,
  `key` VARCHAR(255) NOT NULL UNIQUE,
  subscription_type VARCHAR(50) DEFAULT 'Basic',  -- NEW!
  used TINYINT(1) DEFAULT 0,
  created_at BIGINT NOT NULL,
  created_by INT,
  used_at BIGINT,
  used_by INT,
  FOREIGN KEY(created_by) REFERENCES users(id) ON DELETE SET NULL,
  FOREIGN KEY(used_by) REFERENCES users(id) ON DELETE SET NULL
)
```

## Логика работы

### Генерация ключа

1. Админ выбирает тип подписки (Basic/Plus/Lifetime)
2. Система генерирует уникальный ключ в формате XXXX-XXXX-XXXX
3. Ключ сохраняется в БД с полями:
   - `key` - сам ключ
   - `subscription_type` - тип подписки
   - `used = 0` - не использован
   - `created_by` - ID админа
   - `created_at` - время создания

### Активация ключа

1. Пользователь вводит ключ
2. Система проверяет:
   - Существует ли ключ
   - `used = 0` (не использован)
3. Если ключ валиден:
   - Помечает ключ: `used = 1`
   - Записывает `used_by` (ID пользователя) и `used_at` (время)
   - Выдает подписку в зависимости от `subscription_type`:
     - **Basic**: `expires_at = now + 30 дней`
     - **Plus**: `expires_at = now + 90 дней`
     - **Lifetime**: `expires_at = 0` (навсегда)

### Проверка подписки

При каждом запросе система проверяет:
```javascript
function isSubscriptionActive(user) {
  const tier = user.subscription_tier
  const expires = user.subscription_expires_at
  
  if (tier === 'None') return false
  if (expires === 0) return true  // Lifetime
  return expires > Date.now()
}
```

## Безопасность

✅ **Однократная активация**
- Ключ можно использовать только 1 раз
- После активации `used = 1` и ключ блокируется
- Невозможно повторно активировать использованный ключ

✅ **Логирование**
- Кто создал ключ (`created_by`)
- Когда создан (`created_at`)
- Кто использовал (`used_by`)
- Когда использован (`used_at`)

✅ **Права доступа**
- Только админы могут генерировать ключи
- Любой авторизованный пользователь может активировать ключ
- Ключи генерируются криптографически безопасным способом (crypto.randomBytes)

## Примеры использования

### Промо-кампания

```bash
# Генерируем 50 Basic ключей для раздачи
node generate-multiple-keys.js
# Выбираем: 1 (Basic)
# Вводим: 50
```

### Продажа подписок

```bash
# Basic - $5/месяц
node generate-key-with-type.js
# Выбираем: 1 (Basic)

# Plus - $12/3 месяца
node generate-key-with-type.js
# Выбираем: 2 (Plus)

# Lifetime - $50/навсегда
node generate-key-with-type.js
# Выбираем: 3 (Lifetime)
```

### Проверка статистики

```bash
node show-keys-mysql.js
```

Вывод:
```
📊 Статистика:
   Всего ключей: 3
   Доступно: 2
   Использовано: 1

   По типам:
   - Basic (30 дней): 1 (доступно: 0)
   - Plus (90 дней): 1 (доступно: 1)
   - Lifetime (навсегда): 1 (доступно: 1)
```

## Миграция существующих ключей

Все существующие ключи автоматически получат тип `Basic` при обновлении базы данных.

Для изменения типа существующего ключа:

```sql
UPDATE license_keys 
SET subscription_type = 'Lifetime' 
WHERE `key` = 'A1B2-C3D4-E5F6';
```

## Что дальше?

### Интеграция с админ-панелью

В админ-панели можно добавить:
1. Выпадающий список для выбора типа подписки
2. Таблицу с ключами и их типами
3. Фильтры по типу подписки
4. Статистику по использованию ключей

### Дополнительные функции

Можно добавить:
1. Экспорт ключей в CSV/Excel
2. Массовое удаление неиспользованных ключей
3. Уведомления об истечении подписки
4. История активаций ключей
5. Ограничение количества активаций на пользователя

## Поддержка

Если возникли проблемы:
1. Проверьте логи сервера
2. Убедитесь, что база данных обновлена (колонка `subscription_type` добавлена)
3. Проверьте права доступа (только админы могут генерировать ключи)
4. Используйте `show-keys-mysql.js` для проверки состояния ключей

## Заключение

Система лицензионных ключей полностью готова к использованию:

✅ Выбор типа подписки (Basic/Plus/Lifetime)
✅ Однократная активация ключа
✅ Автоматическое управление сроками подписок
✅ Полное логирование всех действий
✅ Безопасная генерация ключей
✅ Удобные скрипты для управления
✅ Подробная документация

**Система протестирована и работает корректно!**
