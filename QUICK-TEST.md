# 🚀 Быстрое тестирование интеграции

## Запуск

### Терминал 1: Сервер
```bash
cd "Site For NightDLC"
npm run api
```

### Терминал 2: Лаунчер
```bash
cd NelonLauncher
npm start
```

---

## Тест 1: Регистрация (2 минуты)

1. Запустите лаунчер
2. Нажмите **"Register"**
3. Заполните:
   - Username: `test1`
   - Email: `test1@test.com`
   - Password: `123456`
   - Confirm: `123456`
4. Нажмите **"Create Account"**

**✅ Ожидается**: Автоматический вход, главный экран

---

## Тест 2: Вход (1 минута)

1. Нажмите **Logout** (иконка выхода)
2. Введите:
   - Username: `test1`
   - Password: `123456`
3. Нажмите **"Continue"**

**✅ Ожидается**: Успешный вход

---

## Тест 3: Подписка (2 минуты)

### Без подписки
1. Проверьте кнопку **"Launch"**
2. **✅ Ожидается**: Кнопка серая (неактивна)

### С подпиской
```bash
cd "Site For NightDLC"
node give-elite.js test1
```

3. Перезапустите лаунчер или выйдите/войдите
4. **✅ Ожидается**: 
   - Кнопка "Launch" активна
   - Отображается "Subscription: Elite"

---

## Тест 4: HWID (3 минуты)

1. Откройте админ-панель: http://localhost:5174/admin
2. Войдите как `admin` / `admin`
3. Найдите пользователя `test1`
4. Нажмите **"Reset HWID"**
5. Попробуйте войти в лаунчер
6. **✅ Ожидается**: Успешный вход с новым HWID

---

## Тест 5: Валидация (2 минуты)

### Регистрация
- Пустые поля → **Ошибка**
- Пароли не совпадают → **Ошибка**
- Пароль < 6 символов → **Ошибка**
- Существующий username → **"exists"**

### Вход
- Неверный пароль → **"Invalid credentials"**
- Несуществующий пользователь → **"Invalid credentials"**

---

## Быстрая проверка API

```bash
# Регистрация
curl -X POST http://localhost:5174/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{"username":"test2","email":"test2@test.com","password":"123456"}'

# Вход
curl -X POST http://localhost:5174/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"login":"test2","password":"123456","hwid":"test-hwid"}'

# Проверка токена (замените TOKEN)
curl http://localhost:5174/api/me \
  -H "Authorization: Bearer TOKEN"
```

---

## Проверка базы данных

```bash
cd "Site For NightDLC"
node show-users.js
```

**✅ Ожидается**: Список пользователей с HWID

---

## Все работает? ✅

Если все тесты прошли успешно:

1. ✅ Регистрация работает
2. ✅ Вход работает
3. ✅ HWID привязка работает
4. ✅ Проверка подписки работает
5. ✅ Валидация работает

**Готово к деплою! 🚀**

---

## Проблемы?

### "Cannot connect to server"
- Проверьте, что сервер запущен
- Проверьте порт 5174

### "Invalid credentials"
- Проверьте правильность username/password
- Проверьте базу данных: `node show-users.js`

### Кнопка Launch неактивна
- Выдайте подписку: `node give-elite.js USERNAME`

### Другие проблемы
- Проверьте логи сервера
- Откройте DevTools в лаунчере (Ctrl+Shift+I)
- Проверьте консоль браузера
