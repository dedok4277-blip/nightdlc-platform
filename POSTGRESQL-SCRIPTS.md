# PostgreSQL Scripts - Инструкция

## Доступные скрипты для работы с PostgreSQL

### 1. Тестирование подключения

Проверяет подключение к PostgreSQL базе данных:

```bash
npm run test:postgres
```

**Что проверяет:**
- Наличие `DATABASE_URL` в `.env`
- Подключение к базе данных
- Версию PostgreSQL
- Список существующих таблиц

**Пример вывода:**
```
🔍 Проверка подключения к PostgreSQL...
📍 DATABASE_URL найден
🔌 Подключение к базе данных...
✅ Подключение успешно!
📊 Информация о базе данных:
   PostgreSQL: PostgreSQL 16.1
📋 Проверка таблиц:
   Найдено таблиц: 4
   - users
   - posts
   - post_likes
   - license_keys
✅ Тест завершен успешно!
```

---

### 2. Просмотр пользователей

Показывает всех пользователей в базе данных:

```bash
npm run users:postgres
```

**Что показывает:**
- UID и имя пользователя
- Email
- Статус админа
- Тип подписки и срок действия
- HWID (если привязан)
- Дату создания и последнего входа

**Пример вывода:**
```
📊 Пользователи в базе данных:

1. admin (UID: 1)
   Email: admin@nelondlc.local
   Админ: Да
   Подписка: None
   Создан: 02.02.2026, 15:30:00

2. testuser (UID: 2)
   Email: test@example.com
   Админ: Нет
   Подписка: Elite
   Истекает: Никогда (вечная)
   HWID: ABC123-DEF456
   Создан: 02.02.2026, 16:00:00
   Последний вход: 02.02.2026, 17:30:00

Всего пользователей: 2
```

---

### 3. Выдача прав администратора

Делает пользователя администратором:

```bash
npm run admin:postgres username
```

**Или напрямую:**
```bash
node give-admin-postgres.js username
```

**Пример:**
```bash
npm run admin:postgres testuser
# ✅ Пользователю "testuser" (UID: 2) выданы права администратора
```

---

### 4. Выдача Elite подписки

Выдает пользователю вечную Elite подписку:

```bash
npm run elite:postgres username
```

**Или напрямую:**
```bash
node give-elite-postgres.js username
```

**Пример:**
```bash
npm run elite:postgres testuser
# ✅ Пользователю "testuser" (UID: 2) выдана вечная Elite подписка
```

---

## Настройка для локального использования

Чтобы использовать эти скрипты локально, добавьте `DATABASE_URL` в `.env`:

```bash
# .env
DATABASE_URL=postgresql://user:pass@host:5432/database
```

**Важно:** Для локальной разработки приложения НЕ добавляйте `DATABASE_URL` - будет использоваться SQLite. Добавляйте только для работы со скриптами PostgreSQL.

---

## Использование на Render

Эти скрипты можно запустить через Render Shell:

1. Откройте ваш Web Service на Render
2. Перейдите в **Shell**
3. Выполните команду:

```bash
npm run users:postgres
npm run admin:postgres username
npm run elite:postgres username
```

---

## Решение проблем

### Ошибка: DATABASE_URL не найден

Добавьте `DATABASE_URL` в `.env` файл:
```bash
DATABASE_URL=postgresql://...
```

### Ошибка подключения

1. Проверьте правильность `DATABASE_URL`
2. Убедитесь, что база данных доступна
3. Используйте **Internal Database URL** из Render

### Пользователь не найден

Проверьте список пользователей:
```bash
npm run users:postgres
```

---

## Дополнительные скрипты

### Прямое подключение через psql

Если у вас установлен PostgreSQL клиент:

```bash
psql "postgresql://user:pass@host:5432/database"
```

### SQL запросы

```sql
-- Все пользователи
SELECT * FROM users;

-- Админы
SELECT username, email FROM users WHERE is_admin = 1;

-- Пользователи с подпиской
SELECT username, subscription_tier, subscription_expires_at 
FROM users 
WHERE subscription_tier != 'None';

-- Статистика
SELECT 
  COUNT(*) as total_users,
  SUM(CASE WHEN is_admin = 1 THEN 1 ELSE 0 END) as admins,
  SUM(CASE WHEN subscription_tier = 'Elite' THEN 1 ELSE 0 END) as elite_users
FROM users;
```

---

## Безопасность

⚠️ **Важно:**
- Не коммитьте `.env` файл с `DATABASE_URL`
- Используйте сильные пароли для базы данных
- Регулярно делайте бэкапы
- Ограничьте доступ к базе данных только для необходимых IP

---

## Поддержка

Если возникли проблемы:
1. Проверьте логи: `npm run test:postgres`
2. Убедитесь, что база данных запущена на Render
3. Проверьте правильность `DATABASE_URL`
