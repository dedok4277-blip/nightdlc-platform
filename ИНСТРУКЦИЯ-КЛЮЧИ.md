# 🔑 Система лицензионных ключей - Краткая инструкция

## Что изменилось?

✅ Теперь можно выбирать тип подписки при генерации ключа:
- **Basic** - 30 дней
- **Plus** - 90 дней  
- **Lifetime** - навсегда

✅ Каждый ключ можно активировать только **ОДИН РАЗ**

## Быстрый старт

### 1. Сгенерировать один ключ

```bash
node generate-key-with-type.js
```

Выберите тип:
- `1` - Basic (30 дней)
- `2` - Plus (90 дней)
- `3` - Lifetime (навсегда)

### 2. Сгенерировать много ключей

```bash
node generate-multiple-keys.js
```

Выберите тип и количество (до 100 штук).

### 3. Посмотреть все ключи

```bash
node show-keys.js
```

Покажет все ключи, их статус и статистику.

## Типы подписок

| Тип | Срок | Описание |
|-----|------|----------|
| Basic | 30 дней | Подписка на месяц |
| Plus | 90 дней | Подписка на 3 месяца |
| Lifetime | Навсегда | Вечная подписка |

## Активация ключа

Пользователь вводит ключ в личном кабинете:
1. Заходит в профиль
2. Вводит ключ
3. Получает подписку в зависимости от типа ключа

⚠️ **Важно**: Ключ можно использовать только один раз!

## Примеры

### Создать 10 ключей Basic для раздачи

```bash
node generate-multiple-keys.js
# Выбираем: 1 (Basic)
# Вводим: 10
```

### Создать 1 Lifetime ключ для продажи

```bash
node generate-key-with-type.js
# Выбираем: 3 (Lifetime)
```

### Проверить статистику

```bash
node show-keys.js
```

Вывод:
```
📊 Статистика:
   Всего ключей: 15
   Доступно: 12
   Использовано: 3

   По типам:
   - Basic (30 дней): 10 (доступно: 8)
   - Plus (90 дней): 3 (доступно: 2)
   - Lifetime (навсегда): 2 (доступно: 2)
```

## API для админ-панели

### Генерация ключа

```javascript
POST /api/admin/generate-key
{
  "subscriptionType": "Lifetime"  // Basic, Plus или Lifetime
}
```

### Просмотр всех ключей

```javascript
GET /api/admin/keys
```

### Удаление ключа

```javascript
DELETE /api/admin/keys/:id
```

## Что происходит при активации?

1. Система проверяет, что ключ существует и не использован
2. Помечает ключ как использованный
3. Выдает подписку:
   - **Basic**: текущая дата + 30 дней
   - **Plus**: текущая дата + 90 дней
   - **Lifetime**: навсегда (без срока)

## Безопасность

✅ Ключ можно активировать только 1 раз
✅ После активации ключ блокируется
✅ Все действия логируются
✅ Только админы могут генерировать ключи

## Обновление базы данных

База данных обновляется автоматически при запуске сервера. Добавляется колонка `subscription_type` в таблицу `license_keys`.

Если нужно обновить вручную:

**MySQL:**
```sql
ALTER TABLE license_keys 
ADD COLUMN subscription_type VARCHAR(50) DEFAULT 'Basic' AFTER `key`;
```

**PostgreSQL:**
```sql
ALTER TABLE license_keys 
ADD COLUMN subscription_type VARCHAR(50) DEFAULT 'Basic';
```

## Старые ключи

Все существующие ключи автоматически получат тип `Basic`.

## Полная документация

Смотрите `KEYS-SYSTEM-GUIDE.md` для подробной информации.
