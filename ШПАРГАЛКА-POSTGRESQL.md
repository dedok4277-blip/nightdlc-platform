# Шпаргалка по PostgreSQL

## 🚀 Быстрый старт

### 1. Получите DATABASE_URL из Render
```
postgresql://user:pass@dpg-xxxxx.oregon-postgres.render.com/nelondlc_db
```

### 2. Добавьте в Environment на Render
```bash
DATABASE_URL=postgresql://...
NODE_ENV=production
JWT_SECRET=ваш-секретный-ключ
```

### 3. Деплой
Render автоматически перезапустит приложение.

## 📝 Полезные команды

### Тестирование подключения
```bash
npm run test:postgres
```

### Просмотр пользователей
```bash
npm run users:postgres
```

### Выдать права админа
```bash
npm run admin:postgres username
# или
node give-admin-postgres.js username
```

### Выдать Elite подписку
```bash
npm run elite:postgres username
# или
node give-elite-postgres.js username
```

## 🔑 Админ по умолчанию

После первого запуска создается:
- **Логин:** admin
- **Пароль:** admin

⚠️ **Сразу смените пароль!**

## 🗄️ Структура базы данных

### Таблицы:
- `users` - пользователи
- `posts` - посты на форуме
- `post_likes` - лайки постов
- `license_keys` - лицензионные ключи

Все таблицы создаются автоматически при первом запуске.

## 🔧 Локальная разработка

Для локальной работы **НЕ добавляйте** `DATABASE_URL` в `.env`:
```bash
# .env (локально)
PORT=5173
NODE_ENV=development
JWT_SECRET=local-secret-key
# DATABASE_URL не указываем!
```

Приложение автоматически будет использовать SQLite.

## 📊 Проверка в Render

### Логи должны показать:
```
✅ Using PostgreSQL database
✅ PostgreSQL tables initialized
✅ Admin user created: admin / admin
✅ NelonDLC API listening on http://0.0.0.0:10000
```

### Тест API:
```bash
curl https://ваш-домен.onrender.com/api/health
# Ответ: {"ok":true}
```

## 🐛 Решение проблем

### Ошибка подключения
1. Проверьте `DATABASE_URL` - должен быть **Internal**, а не External
2. Убедитесь, что база данных запущена (Available)
3. Проверьте логи на Render

### Нет таблиц
Таблицы создаются автоматически. Если их нет:
1. Проверьте логи на ошибки
2. Убедитесь, что `DATABASE_URL` правильный

### Нет админа
1. Проверьте логи - должно быть "Admin user created"
2. Если нет, создайте вручную через регистрацию
3. Выдайте права: `npm run admin:postgres username`

## 📚 Документация

- `DEPLOY-POSTGRESQL.md` - полная инструкция по деплою
- `SETUP-POSTGRESQL.md` - детальная настройка
- `POSTGRESQL-RENDER.md` - краткая инструкция

## 💡 Советы

1. **Используйте Internal Database URL** - он быстрее и безопаснее
2. **Смените пароль админа** сразу после первого входа
3. **Делайте бэкапы** базы данных регулярно
4. **Мониторьте логи** на Render для отслеживания ошибок
