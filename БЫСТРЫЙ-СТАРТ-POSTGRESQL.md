# 🚀 Быстрый старт с PostgreSQL на Render

## 3 простых шага

### Шаг 1: Получите DATABASE_URL

1. Откройте базу данных **nelondlc-db** на Render
2. Вкладка **Info** → скопируйте **Internal Database URL**

```
postgresql://user:pass@dpg-xxxxx.oregon-postgres.render.com/nelondlc_db
```

### Шаг 2: Добавьте переменные окружения

В вашем Web Service → **Environment** → добавьте:

```bash
DATABASE_URL=postgresql://user:pass@dpg-xxxxx.oregon-postgres.render.com/nelondlc_db
NODE_ENV=production
JWT_SECRET=ваш-секретный-ключ-минимум-32-символа
```

**Генерация JWT_SECRET:**
```bash
node generate-key.js
```

### Шаг 3: Деплой

Сохраните переменные → Render автоматически запустит деплой.

---

## ✅ Проверка

### В логах должно быть:
```
✅ Using PostgreSQL database
✅ PostgreSQL tables initialized
✅ Admin user created: admin / admin
```

### Тест API:
```bash
curl https://ваш-домен.onrender.com/api/health
# Ответ: {"ok":true}
```

### Вход в админку:
- Логин: `admin`
- Пароль: `admin`
- **Сразу смените пароль!**

---

## 📚 Дополнительная документация

- `CHECKLIST-POSTGRESQL.md` - полный чеклист
- `DEPLOY-POSTGRESQL.md` - детальная инструкция
- `ШПАРГАЛКА-POSTGRESQL.md` - шпаргалка по командам
- `POSTGRESQL-SCRIPTS.md` - описание скриптов

---

## 🔧 Полезные команды

```bash
# Просмотр пользователей
npm run users:postgres

# Выдать админку
npm run admin:postgres username

# Выдать Elite подписку
npm run elite:postgres username

# Тест подключения
npm run test:postgres
```

---

## 🐛 Проблемы?

1. **Не подключается к БД** → Проверьте, что используете **Internal** URL
2. **Нет таблиц** → Они создаются автоматически, проверьте логи
3. **Нет админа** → Создайте через регистрацию и выдайте права

---

## 🎉 Готово!

Ваше приложение работает с PostgreSQL на Render!
