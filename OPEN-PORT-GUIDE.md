# 🌐 Как открыть порты для доступа к сайту из интернета

Полная инструкция для Windows, чтобы люди могли заходить на ваш сайт через интернет.

---

## ⚠️ ВАЖНО: Прочитайте перед началом

### Это решение НЕ подходит для продакшена, потому что:
- 🔴 **Небезопасно** - ваш компьютер открыт для атак
- 🔴 **Нестабильно** - если компьютер выключен, сайт не работает
- 🔴 **Медленно** - домашний интернет медленнее серверов
- 🔴 **Дорого** - компьютер должен работать 24/7 (электричество)
- 🔴 **IP меняется** - нужно постоянно обновлять адрес

### Используйте только для:
- ✅ Тестирования с друзьями
- ✅ Демонстрации проекта
- ✅ Временного доступа

### Для настоящего сайта используйте:
- ✅ **Render.com** (см. `DEPLOY-RENDER.md`)
- ✅ **Vercel, Netlify, Railway**
- ✅ **VPS сервер**

---

## 📋 Что нужно

1. Windows 10/11
2. XAMPP установлен и запущен
3. Доступ к настройкам роутера
4. Статический IP от провайдера (или DynDNS)

---

## 🔥 Шаг 1: Настройка Windows Firewall

### 1.1 Откройте порт 80 (HTTP)

1. Нажмите **Win + R**
2. Введите: `wf.msc` и нажмите Enter
3. Откроется **Брандмауэр Windows в режиме повышенной безопасности**

### 1.2 Создайте правило для входящих подключений

1. Слева выберите **"Правила для входящих подключений"**
2. Справа нажмите **"Создать правило..."**
3. Выберите **"Для порта"** → Далее
4. Выберите **TCP**
5. Выберите **"Определенные локальные порты"**
6. Введите: `80` (для HTTP)
7. Нажмите **Далее**
8. Выберите **"Разрешить подключение"** → Далее
9. Отметьте все профили (Доменный, Частный, Публичный) → Далее
10. Имя: `XAMPP HTTP Port 80`
11. Нажмите **Готово**

### 1.3 Создайте правило для порта 443 (HTTPS)

Повторите шаги выше, но:
- Порт: `443`
- Имя: `XAMPP HTTPS Port 443`

### 1.4 Создайте правило для порта 3306 (MySQL)

Повторите шаги выше, но:
- Порт: `3306`
- Имя: `XAMPP MySQL Port 3306`

### 1.5 Создайте правило для вашего Node.js сервера

Если ваш сайт работает на порту 5173:
- Порт: `5173`
- Имя: `Node.js Server Port 5173`

---

## 🌐 Шаг 2: Настройка XAMPP

### 2.1 Настройте Apache для внешнего доступа

1. Откройте XAMPP Control Panel
2. Нажмите **Config** напротив Apache
3. Выберите **httpd.conf**
4. Найдите строку:
   ```apache
   Listen 80
   ```
5. Убедитесь, что она не содержит `127.0.0.1`:
   ```apache
   Listen 80
   # НЕ Listen 127.0.0.1:80
   ```

### 2.2 Настройте MySQL для внешнего доступа

1. В XAMPP Control Panel нажмите **Config** напротив MySQL
2. Выберите **my.ini**
3. Найдите строку:
   ```ini
   bind-address = 127.0.0.1
   ```
4. Замените на:
   ```ini
   bind-address = 0.0.0.0
   ```
5. Сохраните файл
6. Перезапустите MySQL в XAMPP

### 2.3 Создайте пользователя MySQL для удаленного доступа

1. Откройте phpMyAdmin (http://localhost/phpmyadmin)
2. Перейдите на вкладку **SQL**
3. Выполните:

```sql
-- Создаем пользователя для удаленного доступа
CREATE USER 'remote_user'@'%' IDENTIFIED BY 'Сложный-Пароль-123!';

-- Даем права на вашу базу
GRANT ALL PRIVILEGES ON nelondlc.* TO 'remote_user'@'%';

-- Применяем изменения
FLUSH PRIVILEGES;
```

⚠️ **Важно**: Используйте сложный пароль!

---

## 🏠 Шаг 3: Узнайте ваш локальный IP

### 3.1 Откройте командную строку

1. Нажмите **Win + R**
2. Введите: `cmd` и нажмите Enter

### 3.2 Узнайте IP адрес

Введите команду:
```cmd
ipconfig
```

Найдите раздел **"Адаптер Ethernet"** или **"Беспроводная сеть"**:
```
IPv4-адрес. . . . . . . . . . . . : 192.168.1.100
```

Запишите этот IP (например, `192.168.1.100`)

---

## 🌍 Шаг 4: Узнайте ваш внешний IP

### 4.1 Откройте сайт

Перейдите на: https://whatismyipaddress.com/

### 4.2 Запишите IP

Вы увидите что-то вроде: `123.45.67.89`

Это ваш **внешний IP адрес** - его будут использовать люди для доступа к сайту.

---

## 🔧 Шаг 5: Настройка роутера (Port Forwarding)

Это самый важный шаг! Без него никто не сможет подключиться.

### 5.1 Войдите в настройки роутера

1. Откройте браузер
2. Введите один из адресов:
   - `192.168.1.1`
   - `192.168.0.1`
   - `192.168.100.1`
   - `10.0.0.1`

3. Введите логин и пароль:
   - Обычно: `admin` / `admin`
   - Или смотрите на наклейке роутера

### 5.2 Найдите раздел Port Forwarding

Ищите разделы с названиями:
- **Port Forwarding**
- **Проброс портов**
- **Virtual Server**
- **NAT**
- **Переадресация портов**

### 5.3 Добавьте правила

Создайте следующие правила:

#### Правило 1: HTTP (порт 80)
- **Имя**: `XAMPP-HTTP`
- **Внешний порт**: `80`
- **Внутренний порт**: `80`
- **Внутренний IP**: `192.168.1.100` (ваш локальный IP из Шага 3)
- **Протокол**: `TCP`
- **Включено**: ✅

#### Правило 2: HTTPS (порт 443)
- **Имя**: `XAMPP-HTTPS`
- **Внешний порт**: `443`
- **Внутренний порт**: `443`
- **Внутренний IP**: `192.168.1.100`
- **Протокол**: `TCP`
- **Включено**: ✅

#### Правило 3: MySQL (порт 3306)
- **Имя**: `XAMPP-MySQL`
- **Внешний порт**: `3306`
- **Внутренний порт**: `3306`
- **Внутренний IP**: `192.168.1.100`
- **Протокол**: `TCP`
- **Включено**: ✅

#### Правило 4: Node.js (порт 5173)
- **Имя**: `NodeJS-Server`
- **Внешний порт**: `5173`
- **Внутренний порт**: `5173`
- **Внутренний IP**: `192.168.1.100`
- **Протокол**: `TCP`
- **Включено**: ✅

### 5.4 Сохраните настройки

Нажмите **Применить** или **Сохранить**

Роутер может перезагрузиться (1-2 минуты).

---

## ✅ Шаг 6: Проверка доступа

### 6.1 Проверка с другого устройства в локальной сети

На телефоне или другом компьютере в той же сети:

1. Откройте браузер
2. Введите: `http://192.168.1.100` (ваш локальный IP)
3. Должен открыться ваш сайт

### 6.2 Проверка из интернета

Попросите друга или используйте мобильный интернет (отключите WiFi):

1. Откройте браузер
2. Введите: `http://123.45.67.89` (ваш внешний IP)
3. Должен открыться ваш сайт

Если используете порт 5173:
```
http://123.45.67.89:5173
```

---

## 🔒 Шаг 7: Безопасность (ОБЯЗАТЕЛЬНО!)

### 7.1 Измените пароли

В phpMyAdmin:
```sql
-- Измените пароль root
ALTER USER 'root'@'localhost' IDENTIFIED BY 'Новый-Сложный-Пароль-123!';
FLUSH PRIVILEGES;
```

### 7.2 Ограничьте доступ к phpMyAdmin

Отредактируйте `C:\xampp\apache\conf\extra\httpd-xampp.conf`:

```apache
<Directory "C:/xampp/phpMyAdmin">
    AllowOverride AuthConfig
    Require ip 127.0.0.1
    # Добавьте только доверенные IP
    # Require ip 123.45.67.89
</Directory>
```

### 7.3 Включите HTTPS

1. В XAMPP Control Panel нажмите **Config** → **Apache (httpd-ssl.conf)**
2. Настройте SSL сертификат
3. Или используйте Let's Encrypt: https://letsencrypt.org/

### 7.4 Настройте fail2ban (защита от брутфорса)

Установите fail2ban для Windows или используйте встроенные средства защиты.

---

## 🌐 Шаг 8: Настройка доменного имени (опционально)

Вместо IP адреса можно использовать домен.

### Вариант 1: Купить домен

1. Купите домен на:
   - **Reg.ru** (для России)
   - **Namecheap.com**
   - **GoDaddy.com**

2. В настройках DNS добавьте A-запись:
   ```
   Тип: A
   Имя: @
   Значение: 123.45.67.89 (ваш внешний IP)
   TTL: 3600
   ```

### Вариант 2: Бесплатный DynDNS (если IP меняется)

Если ваш провайдер меняет IP адрес:

1. Зарегистрируйтесь на:
   - **No-IP.com** (бесплатно)
   - **DuckDNS.org** (бесплатно)
   - **Dynu.com** (бесплатно)

2. Создайте hostname: `ваш-сайт.ddns.net`

3. Установите клиент для автообновления IP:
   - Скачайте DUC (Dynamic Update Client)
   - Настройте автозапуск

4. Теперь можно использовать: `http://ваш-сайт.ddns.net`

---

## 🐛 Решение проблем

### Проблема 1: Сайт не открывается из интернета

**Проверьте**:
1. Порты открыты в Windows Firewall
2. Port Forwarding настроен в роутере
3. XAMPP запущен
4. Используете правильный внешний IP
5. Провайдер не блокирует порты (некоторые блокируют 80 и 443)

**Решение**: Используйте другой порт (например, 8080):
```
http://123.45.67.89:8080
```

### Проблема 2: IP адрес постоянно меняется

**Решение**: Используйте DynDNS (см. Шаг 8, Вариант 2)

### Проблема 3: Провайдер блокирует порты

**Проверка**:
```cmd
telnet ваш-внешний-ip 80
```

Если не подключается - провайдер блокирует.

**Решение**:
1. Используйте нестандартные порты (8080, 8443)
2. Или используйте VPN/туннель (ngrok, localtunnel)
3. Или перейдите на облачный хостинг

### Проблема 4: Медленная работа

**Причины**:
- Медленный домашний интернет (особенно upload)
- Слабый компьютер
- Много одновременных подключений

**Решение**: Используйте облачный хостинг (Render, Vercel)

---

## 🚀 Альтернатива: Временный доступ через туннель

Если не хотите настраивать роутер, используйте туннель:

### Вариант 1: ngrok (самый простой)

1. Скачайте: https://ngrok.com/download
2. Распакуйте в папку
3. Зарегистрируйтесь и получите токен
4. Запустите:
   ```cmd
   ngrok.exe authtoken ваш-токен
   ngrok.exe http 80
   ```
5. Получите публичный URL: `https://abc123.ngrok.io`

**Плюсы**:
- ✅ Не нужно настраивать роутер
- ✅ Автоматический HTTPS
- ✅ Работает за любым файрволом

**Минусы**:
- ⚠️ Бесплатно только 1 туннель
- ⚠️ URL меняется при перезапуске
- ⚠️ Ограничение по трафику

### Вариант 2: localtunnel

```cmd
npm install -g localtunnel
lt --port 80 --subdomain ваш-сайт
```

Получите: `https://ваш-сайт.loca.lt`

### Вариант 3: Cloudflare Tunnel

1. Установите: https://developers.cloudflare.com/cloudflare-one/connections/connect-apps/install-and-setup/
2. Запустите:
   ```cmd
   cloudflared tunnel --url http://localhost:80
   ```

---

## 📊 Сравнение методов

| Метод | Сложность | Стабильность | Безопасность | Стоимость |
|-------|-----------|--------------|--------------|-----------|
| **Port Forwarding** | ⭐⭐⭐⭐ | ⭐⭐⭐ | ⚠️ Средняя | 💰 Бесплатно |
| **ngrok** | ⭐ | ⭐⭐⭐⭐ | ✅ Высокая | 💰 $8/мес |
| **Cloudflare Tunnel** | ⭐⭐ | ⭐⭐⭐⭐⭐ | ✅ Высокая | 💰 Бесплатно |
| **Render.com** | ⭐⭐ | ⭐⭐⭐⭐⭐ | ✅ Высокая | 💰 Бесплатно |

---

## 🎯 Рекомендация

### Для тестирования (1-2 дня):
✅ Используйте **ngrok** или **localtunnel**

### Для демонстрации (1-2 недели):
✅ Настройте **Port Forwarding** + **DynDNS**

### Для настоящего сайта:
✅ Используйте **Render.com** или другой облачный хостинг

---

## 📝 Чеклист

- [ ] Открыты порты в Windows Firewall
- [ ] XAMPP настроен для внешнего доступа
- [ ] MySQL настроен для удаленного подключения
- [ ] Узнан локальный IP адрес
- [ ] Узнан внешний IP адрес
- [ ] Настроен Port Forwarding в роутере
- [ ] Проверен доступ из локальной сети
- [ ] Проверен доступ из интернета
- [ ] Изменены пароли по умолчанию
- [ ] Настроен DynDNS (если IP динамический)

---

## 🆘 Нужна помощь?

Если что-то не работает:

1. Проверьте логи Apache: `C:\xampp\apache\logs\error.log`
2. Проверьте логи MySQL: `C:\xampp\mysql\data\mysql_error.log`
3. Используйте онлайн проверку портов: https://www.yougetsignal.com/tools/open-ports/
4. Проверьте настройки роутера еще раз

---

## ⚠️ Финальное предупреждение

Открытие портов на домашнем компьютере - это риск!

**Для продакшена ВСЕГДА используйте**:
- ✅ Облачный хостинг (Render, Vercel, Railway)
- ✅ VPS сервер с правильной настройкой безопасности
- ✅ Профессиональный хостинг

**НЕ используйте домашний компьютер** для настоящего сайта с реальными пользователями!
