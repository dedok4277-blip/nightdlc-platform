# 🚀 Быстрый старт деплоя на Timeweb

## Вариант 1: Через панель Timeweb (Рекомендуется)

### Шаг 1: Подготовка
```bash
npm run build
```

### Шаг 2: Загрузка файлов
1. Откройте FileZilla или WinSCP
2. Подключитесь к серверу:
   - Хост: `cz505339.tw1.ru` или IP сервера
   - Пользователь: `cz505339`
   - Пароль: ваш пароль от панели
   - Порт: 21 (FTP) или 22 (SFTP)

3. Загрузите ВСЕ файлы КРОМЕ:
   - `node_modules/`
   - `.git/`
   - `*.log`

### Шаг 3: Настройка на сервере
Подключитесь по SSH:
```bash
ssh cz505339@cz505339.tw1.ru
```

Перейдите в директорию сайта:
```bash
cd ~/cz505339.tw1.ru/public_html
```

Установите зависимости:
```bash
npm install --production
```

### Шаг 4: Запуск через PM2
```bash
# Установите PM2 (если еще не установлен)
npm install -g pm2

# Запустите приложение
pm2 start ecosystem.config.js

# Сохраните конфигурацию
pm2 save

# Настройте автозапуск
pm2 startup
```

### Шаг 5: Проверка
Откройте в браузере: `http://cz505339.tw1.ru`

---

## Вариант 2: Через Git (Если есть доступ)

### На сервере:
```bash
cd ~/cz505339.tw1.ru/public_html
git clone <ваш-репозиторий> .
npm install --production
npm run build
pm2 start ecosystem.config.js
```

---

## Настройка порта в панели Timeweb

1. Войдите в панель управления Timeweb
2. Перейдите в "Сайты" → "Основной сайт"
3. В настройках найдите "Проксирование"
4. Укажите порт: `5173`
5. Сохраните изменения

---

## Полезные команды

### Проверка статуса
```bash
pm2 status
pm2 logs nelondlc
```

### Перезапуск после изменений
```bash
pm2 restart nelondlc
```

### Остановка
```bash
pm2 stop nelondlc
```

---

## Привязка своего домена

1. В панели Timeweb: "Сайты" → "Привязать домен"
2. Укажите ваш домен (например, `nightdlc.com`)
3. У регистратора домена настройте DNS:
   - A-запись: `@` → IP сервера Timeweb
   - A-запись: `www` → IP сервера Timeweb

4. Подождите 1-24 часа для обновления DNS

---

## Проблемы?

### Сайт не открывается
- Проверьте: `pm2 logs nelondlc`
- Убедитесь, что порт 5173 открыт в панели Timeweb

### Ошибка базы данных
```bash
chmod 644 nightdlc.db
chmod 755 .
```

### Нужна помощь?
Проверьте полную инструкцию в файле `DEPLOY.md`
