# 🗄️ Система двух баз данных для NelonDLC

## 🎯 Что это?

Полное решение для работы с **PostgreSQL (Render)** и **MySQL (XAMPP)** одновременно.

### Возможности
- ✅ Работа с двумя БД одновременно
- ✅ Автоматическая синхронизация данных
- ✅ Выбор главной базы данных
- ✅ Резервное копирование
- ✅ Гибкое переключение между БД
- ✅ Полная совместимость с существующим кодом

---

## ⚡ Быстрый старт (3 минуты)

### 1. Настройте .env
```bash
copy .env.dual-example .env
```

Отредактируйте `.env`:
```env
DATABASE_URL=postgresql://user:pass@host.render.com/db
XAMPP_ENABLED=true
PRIMARY_DB=postgres
SYNC_DATABASES=true
```

### 2. Измените server/index.js
```javascript
// Замените:
import pool, { nextUid } from './db-adapter.js'

// На:
import pool, { nextUid } from './db-dual.js'
```

### 3. Создайте БД в XAMPP
1. Откройте http://localhost/phpmyadmin
2. Создайте базу `nelondlc`

### 4. Запустите
```bash
npm run test:dual    # Проверка подключения
npm run check:db     # Проверка статуса
npm start            # Запуск сервера
```

---

## 📚 Документация

### Для начинающих
- **ДВОЙНАЯ-БД.md** - Краткая инструкция на русском (5 минут)
- **DUAL-DB-QUICKSTART.md** - Быстрый старт на английском

### Подробная информация
- **DUAL-DATABASE-SETUP.md** - Полная инструкция по настройке
- **DUAL-DB-DIAGRAM.txt** - Визуальная схема архитектуры
- **DUAL-DB-FILES.md** - Описание всех файлов

### Практика
- **CHECKLIST-DUAL-DB.md** - Чеклист для проверки настройки
- **.env.dual-example** - Пример конфигурации

---

## 🛠️ Созданные файлы

### Основные компоненты
```
server/db-dual.js              # Адаптер для двух БД
.env.dual-example              # Пример конфигурации
```

### Скрипты
```
test-dual-db.js                # Тест подключения
check-databases.js             # Проверка статуса
sync-dual-databases.js         # Ручная синхронизация
```

### Документация
```
README-DUAL-DB.md              # Этот файл
DUAL-DATABASE-SETUP.md         # Подробная инструкция (EN)
ДВОЙНАЯ-БД.md                  # Краткая инструкция (RU)
DUAL-DB-QUICKSTART.md          # Быстрый старт
DUAL-DB-DIAGRAM.txt            # Схема архитектуры
DUAL-DB-FILES.md               # Описание файлов
CHECKLIST-DUAL-DB.md           # Чеклист настройки
```

---

## 🎮 Команды

```bash
# Тестирование
npm run test:dual              # Тест подключения к обеим БД
npm run check:db               # Проверка статуса и синхронизации

# Синхронизация
npm run sync:dual              # Ручная синхронизация данных

# Запуск
npm start                      # Запуск сервера с двумя БД
```

---

## 📊 Режимы работы

### 🌐 Продакшен (Render главная)
```env
PRIMARY_DB=postgres
SYNC_DATABASES=true
XAMPP_ENABLED=true
```
Все запросы → PostgreSQL, копия → MySQL

### 💻 Разработка (XAMPP главная)
```env
PRIMARY_DB=mysql
SYNC_DATABASES=true
DATABASE_URL=postgresql://...
```
Все запросы → MySQL, копия → PostgreSQL

### ☁️ Только облако
```env
PRIMARY_DB=postgres
XAMPP_ENABLED=false
```

### 🏠 Только локально
```env
PRIMARY_DB=mysql
DATABASE_URL=
```

---

## 🔍 Проверка работы

После настройки выполните:

```bash
npm run check:db
```

Вы должны увидеть:
```
✅ PostgreSQL (Render)
   Status: Connected
   Users: 5

✅ MySQL (XAMPP)
   Status: Connected
   Users: 5

✅ Databases are in sync!
```

---

## 🏗️ Архитектура

```
Клиент (React)
      ↓
API (Express)
      ↓
db-dual.js (Адаптер)
      ↓
   ┌──┴──┐
   ↓     ↓
PostgreSQL  MySQL
(Render)   (XAMPP)
```

### Синхронизация
- **Односторонняя**: из главной БД во вторую
- **Автоматическая**: при каждом запросе (если включена)
- **Ручная**: через `npm run sync:dual`

---

## ⚙️ Конфигурация

### Переменные окружения (.env)

```env
# PostgreSQL (Render)
DATABASE_URL=postgresql://user:pass@host/db

# MySQL (XAMPP)
XAMPP_ENABLED=true
XAMPP_HOST=localhost
XAMPP_USER=root
XAMPP_PASSWORD=
XAMPP_DB_NAME=nelondlc
XAMPP_PORT=3306

# Настройки
PRIMARY_DB=postgres              # postgres или mysql
SYNC_DATABASES=true              # true или false
```

---

## 🧪 Тестирование

### 1. Проверка подключения
```bash
npm run test:dual
```

### 2. Проверка статуса
```bash
npm run check:db
```

### 3. Создание тестового пользователя
```bash
# Запустите приложение
npm start

# Зарегистрируйте пользователя через UI
# Проверьте обе БД
```

### 4. Проверка синхронизации
```bash
# Создайте данные в приложении
# Проверьте PostgreSQL (Render Dashboard)
# Проверьте MySQL (phpMyAdmin)
```

---

## 🔧 Устранение проблем

### PostgreSQL не подключается
```bash
# Проверьте DATABASE_URL
echo $DATABASE_URL

# Проверьте подключение
npm run test:postgres
```

### MySQL не подключается
```bash
# Запустите XAMPP
# Проверьте MySQL в XAMPP Control Panel
# Откройте phpMyAdmin: http://localhost/phpmyadmin
```

### Синхронизация не работает
```bash
# Проверьте настройки
npm run check:db

# Ручная синхронизация
npm run sync:dual
```

---

## 📖 Примеры использования

### Пример 1: Продакшен с бэкапом
```env
# Render главная, XAMPP бэкап
DATABASE_URL=postgresql://render-url
XAMPP_ENABLED=true
PRIMARY_DB=postgres
SYNC_DATABASES=true
```

### Пример 2: Локальная разработка
```env
# XAMPP главная, Render бэкап
DATABASE_URL=postgresql://render-url
XAMPP_ENABLED=true
PRIMARY_DB=mysql
SYNC_DATABASES=true
```

### Пример 3: Только облако
```env
# Только PostgreSQL
DATABASE_URL=postgresql://render-url
XAMPP_ENABLED=false
PRIMARY_DB=postgres
```

---

## 🎓 Обучающие материалы

### Начните здесь
1. Прочитайте **ДВОЙНАЯ-БД.md** (5 минут)
2. Следуйте **DUAL-DB-QUICKSTART.md** (10 минут)
3. Используйте **CHECKLIST-DUAL-DB.md** для проверки

### Углубленное изучение
1. **DUAL-DATABASE-SETUP.md** - полная инструкция
2. **DUAL-DB-DIAGRAM.txt** - архитектура системы
3. **DUAL-DB-FILES.md** - описание всех компонентов

---

## ⚠️ Важные замечания

1. **Синхронизация односторонняя** - из главной БД во вторую
2. **Не редактируйте вторую БД** - изменения не синхронизируются обратно
3. **Используйте вторую БД для чтения** - бэкапы, аналитика
4. **Ошибки синхронизации не критичны** - главная БД продолжит работать
5. **Обе БД должны иметь одинаковую структуру** - таблицы создаются автоматически

---

## 🚀 Преимущества

- ✅ **Резервное копирование** - данные в двух местах
- ✅ **Отказоустойчивость** - работа при недоступности одной БД
- ✅ **Гибкость** - легкое переключение между БД
- ✅ **Локальная разработка** - работа без интернета
- ✅ **Облачный бэкап** - автоматическое резервирование
- ✅ **Простота** - минимальные изменения в коде

---

## 📞 Поддержка

### Если что-то не работает:

1. **Проверьте конфигурацию**
   ```bash
   npm run check:db
   ```

2. **Посмотрите логи**
   ```bash
   npm start
   # Смотрите вывод в консоли
   ```

3. **Прочитайте документацию**
   - DUAL-DATABASE-SETUP.md (раздел "Устранение проблем")
   - CHECKLIST-DUAL-DB.md

4. **Проверьте каждую БД отдельно**
   ```bash
   npm run test:postgres
   node test-mysql-connection.js
   ```

---

## 📝 Чеклист готовности

- [ ] Обе БД подключены
- [ ] Синхронизация работает
- [ ] Все тесты проходят
- [ ] Приложение работает
- [ ] Документация изучена

---

## 🎉 Готово!

Теперь у вас есть полноценная система с двумя базами данных!

**Следующие шаги:**
1. Запустите `npm run check:db`
2. Создайте тестовые данные
3. Проверьте синхронизацию
4. Наслаждайтесь работой! 🚀

---

**Версия:** 1.0  
**Дата создания:** 2026-02-02  
**Совместимость:** NelonDLC v0.0.0
