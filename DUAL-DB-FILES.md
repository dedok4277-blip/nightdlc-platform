# 📁 Файлы для работы с двумя базами данных

## 🎯 Созданные файлы

### Основные файлы

1. **server/db-dual.js** - Главный адаптер для работы с двумя БД
   - Подключение к PostgreSQL и MySQL
   - Автоматическая синхронизация
   - Выбор главной БД

2. **.env.dual-example** - Пример конфигурации
   - Настройки PostgreSQL (Render)
   - Настройки MySQL (XAMPP)
   - Параметры синхронизации

### Скрипты для тестирования

3. **test-dual-db.js** - Тест подключения к обеим БД
   ```bash
   npm run test:dual
   ```

4. **check-databases.js** - Проверка статуса и синхронизации
   ```bash
   npm run check:db
   ```

5. **sync-dual-databases.js** - Ручная синхронизация данных
   ```bash
   npm run sync:dual
   ```

### Документация

6. **DUAL-DATABASE-SETUP.md** - Подробная инструкция на английском
   - Пошаговая настройка
   - Режимы работы
   - Устранение проблем

7. **ДВОЙНАЯ-БД.md** - Краткая инструкция на русском
   - Быстрый старт
   - Основные команды

8. **DUAL-DB-QUICKSTART.md** - Быстрый старт
   - Установка за 3 шага
   - Примеры использования

9. **DUAL-DB-DIAGRAM.txt** - Визуальная схема
   - Архитектура системы
   - Потоки данных
   - Конфигурация

10. **DUAL-DB-FILES.md** - Этот файл
    - Список всех файлов
    - Краткое описание

## 🚀 Быстрый старт

### 1. Скопируйте конфигурацию
```bash
copy .env.dual-example .env
```

### 2. Отредактируйте .env
```env
DATABASE_URL=postgresql://your-render-url
XAMPP_ENABLED=true
PRIMARY_DB=postgres
SYNC_DATABASES=true
```

### 3. Измените server/index.js
```javascript
// Замените эту строку:
import pool, { nextUid } from './db-adapter.js'

// На эту:
import pool, { nextUid } from './db-dual.js'
```

### 4. Создайте БД в XAMPP
- Откройте phpMyAdmin
- Создайте базу `nelondlc`

### 5. Проверьте подключение
```bash
npm run test:dual
npm run check:db
```

### 6. Запустите сервер
```bash
npm start
```

## 📊 Новые npm команды

```json
{
  "test:dual": "node test-dual-db.js",
  "check:db": "node check-databases.js",
  "sync:dual": "node sync-dual-databases.js"
}
```

## 📖 Какую документацию читать?

### Для быстрого старта
→ **DUAL-DB-QUICKSTART.md** или **ДВОЙНАЯ-БД.md**

### Для подробной настройки
→ **DUAL-DATABASE-SETUP.md**

### Для понимания архитектуры
→ **DUAL-DB-DIAGRAM.txt**

### Для примера конфигурации
→ **.env.dual-example**

## ⚙️ Структура проекта

```
project/
├── server/
│   ├── index.js           # Основной сервер (измените импорт!)
│   ├── db-dual.js         # ✨ Новый адаптер для двух БД
│   ├── db-adapter.js      # Старый адаптер (SQLite/PostgreSQL)
│   ├── db-postgres.js     # PostgreSQL адаптер
│   └── db-mysql.js        # MySQL адаптер
│
├── .env                   # Ваша конфигурация (создайте!)
├── .env.dual-example      # ✨ Пример конфигурации
│
├── test-dual-db.js        # ✨ Тест подключения
├── check-databases.js     # ✨ Проверка статуса
├── sync-dual-databases.js # ✨ Синхронизация
│
├── DUAL-DATABASE-SETUP.md # ✨ Подробная инструкция
├── ДВОЙНАЯ-БД.md          # ✨ Краткая инструкция (RU)
├── DUAL-DB-QUICKSTART.md  # ✨ Быстрый старт
├── DUAL-DB-DIAGRAM.txt    # ✨ Схема архитектуры
└── DUAL-DB-FILES.md       # ✨ Этот файл
```

## 🔧 Что нужно изменить в существующих файлах?

### 1. server/index.js
```javascript
// Было:
import pool, { nextUid } from './db-adapter.js'

// Стало:
import pool, { nextUid } from './db-dual.js'
```

### 2. .env (создать из .env.dual-example)
```env
DATABASE_URL=postgresql://...
XAMPP_ENABLED=true
PRIMARY_DB=postgres
SYNC_DATABASES=true
```

### 3. package.json (уже обновлен)
Добавлены команды:
- `npm run test:dual`
- `npm run check:db`
- `npm run sync:dual`

## ✅ Проверка установки

После настройки выполните:

```bash
# 1. Проверка подключения
npm run test:dual

# Ожидаемый результат:
# ✅ PostgreSQL (Render): X users
# ✅ MySQL (XAMPP): X users
# ✅ Adapter Query Test: X users found

# 2. Проверка статуса
npm run check:db

# Ожидаемый результат:
# ✅ PostgreSQL (Render)
#    Status: Connected
#    Users: X
# ✅ MySQL (XAMPP)
#    Status: Connected
#    Users: X
# ✅ Databases are in sync!

# 3. Запуск сервера
npm start

# Ожидаемый результат:
# ✅ PostgreSQL (Render) connected
# ✅ MySQL (XAMPP) connected
# 📊 Primary database: postgres
# ✅ PostgreSQL tables initialized
# ✅ MySQL tables initialized
```

## 🎓 Дополнительные ресурсы

- [PostgreSQL Documentation](https://www.postgresql.org/docs/)
- [MySQL Documentation](https://dev.mysql.com/doc/)
- [Render PostgreSQL Guide](https://render.com/docs/databases)
- [XAMPP Documentation](https://www.apachefriends.org/docs/)

## 💡 Советы

1. **Для продакшена**: используйте `PRIMARY_DB=postgres`
2. **Для разработки**: можно использовать `PRIMARY_DB=mysql`
3. **Синхронизация**: включайте только если нужен бэкап
4. **Тестирование**: всегда проверяйте `npm run check:db` после изменений
5. **Бэкапы**: используйте вторую БД для резервных копий

## ⚠️ Важно помнить

- Синхронизация односторонняя (из главной во вторую)
- Не редактируйте вторую БД напрямую
- Ошибки синхронизации не останавливают главную БД
- Обе БД должны иметь одинаковую структуру таблиц

## 🆘 Помощь

Если что-то не работает:

1. Проверьте `.env` файл
2. Запустите `npm run check:db`
3. Посмотрите логи сервера
4. Проверьте подключение к каждой БД отдельно
5. Прочитайте раздел "Устранение проблем" в документации

---

**Готово!** Теперь у вас есть полная система для работы с двумя базами данных. 🎉
