# Настройка двух баз данных одновременно

Этот гайд поможет настроить работу с PostgreSQL (Render) и MySQL (XAMPP) одновременно.

## 🎯 Что это дает?

- **PostgreSQL на Render** - облачная база для продакшена
- **MySQL через XAMPP** - локальная база для разработки и бэкапа
- **Автоматическая синхронизация** - данные копируются между БД
- **Гибкое переключение** - можно выбрать основную БД

## 📋 Шаг 1: Настройка .env файла

Скопируйте `.env.example` в `.env` и заполните:

```env
# PostgreSQL (Render)
DATABASE_URL=postgresql://user:password@dpg-xxxxx.oregon-postgres.render.com/dbname

# MySQL (XAMPP)
XAMPP_ENABLED=true
XAMPP_HOST=localhost
XAMPP_USER=root
XAMPP_PASSWORD=
XAMPP_DB_NAME=nelondlc
XAMPP_PORT=3306

# Основная БД (postgres или mysql)
PRIMARY_DB=postgres

# Синхронизация (true/false)
SYNC_DATABASES=true
```

## 🔧 Шаг 2: Получение DATABASE_URL от Render

1. Зайдите на [Render Dashboard](https://dashboard.render.com/)
2. Откройте вашу PostgreSQL базу
3. Скопируйте **Internal Database URL** или **External Database URL**
4. Вставьте в `.env` как `DATABASE_URL`

## 💾 Шаг 3: Настройка XAMPP

1. Запустите **XAMPP Control Panel**
2. Запустите **Apache** и **MySQL**
3. Откройте **phpMyAdmin** (http://localhost/phpmyadmin)
4. Создайте базу данных `nelondlc`:
   - Нажмите "New" (Создать)
   - Имя: `nelondlc`
   - Кодировка: `utf8mb4_general_ci`
   - Нажмите "Create"

## 🚀 Шаг 4: Изменение server/index.js

Замените импорт базы данных в `server/index.js`:

```javascript
// Было:
import pool, { nextUid } from './db-adapter.js'

// Стало:
import pool, { nextUid } from './db-dual.js'
```

## ⚙️ Режимы работы

### Режим 1: PostgreSQL основная, MySQL бэкап
```env
PRIMARY_DB=postgres
SYNC_DATABASES=true
```
Все запросы идут в PostgreSQL, данные копируются в MySQL.

### Режим 2: MySQL основная, PostgreSQL бэкап
```env
PRIMARY_DB=mysql
SYNC_DATABASES=true
```
Все запросы идут в MySQL, данные копируются в PostgreSQL.

### Режим 3: Только PostgreSQL
```env
PRIMARY_DB=postgres
XAMPP_ENABLED=false
SYNC_DATABASES=false
```

### Режим 4: Только MySQL
```env
PRIMARY_DB=mysql
DATABASE_URL=
SYNC_DATABASES=false
```

## 🧪 Тестирование

Создайте файл `test-dual-db.js`:

```javascript
import dotenv from 'dotenv'
dotenv.config()

import pool from './server/db-dual.js'

async function test() {
  try {
    console.log('🧪 Testing dual database connection...')
    
    const [users] = await pool.query('SELECT COUNT(*) as count FROM users')
    console.log('✅ Users count:', users[0].count)
    
    console.log('✅ Dual database working!')
  } catch (error) {
    console.error('❌ Error:', error.message)
  }
  process.exit(0)
}

test()
```

Запустите:
```bash
node test-dual-db.js
```

## 📊 Проверка синхронизации

1. Создайте пользователя через приложение
2. Проверьте PostgreSQL (через Render Dashboard)
3. Проверьте MySQL (через phpMyAdmin)
4. Данные должны быть в обеих БД

## 🔍 Мониторинг

При запуске сервера вы увидите:
```
✅ PostgreSQL (Render) connected
✅ MySQL (XAMPP) connected
📊 Primary database: postgres
✅ PostgreSQL tables initialized
✅ MySQL tables initialized
```

## ⚠️ Важные замечания

1. **Синхронизация односторонняя** - из основной БД во вторую
2. **Не редактируйте вторую БД напрямую** - изменения не синхронизируются обратно
3. **Используйте вторую БД для чтения** - как бэкап или для аналитики
4. **Проверяйте логи** - ошибки синхронизации не останавливают основную БД

## 🛠️ Устранение проблем

### PostgreSQL не подключается
- Проверьте `DATABASE_URL` в `.env`
- Убедитесь, что БД на Render запущена
- Проверьте SSL настройки

### MySQL не подключается
- Запустите XAMPP
- Проверьте порт (по умолчанию 3306)
- Создайте базу `nelondlc` в phpMyAdmin

### Синхронизация не работает
- Проверьте `SYNC_DATABASES=true` в `.env`
- Смотрите логи сервера на предмет ошибок
- Убедитесь, что обе БД доступны

## 📝 Скрипты для управления

### Показать пользователей в обеих БД
```bash
node show-users-postgres.js  # PostgreSQL
node show-users-mysql.js     # MySQL
```

### Синхронизация данных вручную
```bash
node sync-databases.js
```

## 🎓 Дополнительно

- Для продакшена рекомендуется `PRIMARY_DB=postgres`
- Для локальной разработки можно `PRIMARY_DB=mysql`
- Синхронизация добавляет небольшую задержку к запросам
- Можно отключить синхронизацию для ускорения разработки
