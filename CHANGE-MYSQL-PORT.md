# 🔧 Изменение порта MySQL в XAMPP

## Проблема

MySQL в XAMPP не запускается, потому что порт 3306 занят другим процессом.

## Решение: Изменить порт MySQL

### Вариант 1: Освободить порт 3306 (рекомендуется)

#### Шаг 1: Найдите процесс, занимающий порт

1. Откройте **Командную строку** от администратора
2. Выполните:

```cmd
netstat -ano | findstr :3306
```

Вы увидите что-то вроде:
```
TCP    0.0.0.0:3306    LISTENING    12345
```

Число `12345` - это PID процесса.

#### Шаг 2: Найдите имя процесса

```cmd
tasklist | findstr "12345"
```

(Замените 12345 на ваш PID)

Вы увидите, например:
```
mysqld.exe    12345    Services    0    123,456 K
```

#### Шаг 3: Остановите процесс

Если это другой MySQL сервис:

```cmd
net stop MySQL80
```

Или:

```cmd
net stop MySQL
```

Или через **Диспетчер задач**:
1. Нажмите `Ctrl+Shift+Esc`
2. Вкладка "Службы"
3. Найдите MySQL80 или MySQL
4. ПКМ -> Остановить

#### Шаг 4: Отключите автозапуск

Чтобы служба не запускалась автоматически:

```cmd
sc config MySQL80 start= disabled
```

#### Шаг 5: Запустите MySQL в XAMPP

Откройте XAMPP Control Panel и нажмите **Start** рядом с MySQL.

---

### Вариант 2: Изменить порт XAMPP MySQL на другой

Если вы не можете освободить порт 3306:

#### Шаг 1: Откройте конфигурацию MySQL

1. Откройте **XAMPP Control Panel**
2. Нажмите **Config** рядом с MySQL
3. Выберите **my.ini**

#### Шаг 2: Измените порт

Найдите секцию `[mysqld]` и измените порт:

```ini
[mysqld]
port=3307
```

Также найдите секцию `[client]` и измените:

```ini
[client]
port=3307
```

#### Шаг 3: Сохраните файл

Сохраните `my.ini` и закройте редактор.

#### Шаг 4: Обновите .env

Откройте `Site For NightDLC/.env` и измените:

```env
DB_PORT=3307
```

#### Шаг 5: Перезапустите MySQL

В XAMPP Control Panel:
1. Нажмите **Stop** (если запущен)
2. Нажмите **Start**

---

### Вариант 3: Использовать SQLite (без MySQL)

Самый простой способ для разработки:

#### Шаг 1: Измените импорт в server/index.js

Откройте `Site For NightDLC/server/index.js` и найдите строку:

```javascript
import pool, { nextUid } from './db-mysql.js'
```

Замените на:

```javascript
import pool, { nextUid } from './db-sqlite-backup.js'
```

#### Шаг 2: Перезапустите сервер

```bash
cd "Site For NightDLC"
npm run api
```

SQLite не требует XAMPP! База данных будет в файле `nightdlc.db`.

---

## Проверка

### После изменения порта

1. Откройте XAMPP Control Panel
2. MySQL должен быть **зеленым** (запущен)
3. Проверьте подключение:

```bash
cd "Site For NightDLC"
node test-mysql-connection.js
```

Должно показать:
```
✅ MySQL connection successful!
```

### Запустите сервер

```bash
npm run api
```

Должно показать:
```
✅ NelonDLC API listening on http://localhost:5173
📊 Database: MySQL (cz505339_nelondlc)
```

---

## Если MySQL все еще не запускается

### Проверьте логи

В XAMPP Control Panel:
- Нажмите **Logs** рядом с MySQL
- Выберите **Error log**

Посмотрите последние строки - там будет причина ошибки.

### Запустите от администратора

1. Закройте XAMPP
2. ПКМ на XAMPP Control Panel
3. **Запуск от имени администратора**
4. Попробуйте запустить MySQL

### Проверьте антивирус

Добавьте в исключения:
- `C:\xampp\mysql\bin\mysqld.exe`
- Порт 3306 (или 3307)

---

## Рекомендация

Для **локальной разработки** проще использовать **SQLite**:
- Не требует XAMPP
- Не требует настройки портов
- Работает сразу из коробки

Для **production** (Timeweb) используйте MySQL.

---

## Быстрая помощь

```cmd
# Остановить все MySQL службы
net stop MySQL80
net stop MySQL

# Отключить автозапуск
sc config MySQL80 start= disabled

# Проверить порт
netstat -ano | findstr :3306

# Запустить XAMPP от админа
# ПКМ -> Запуск от имени администратора
```

---

**Дата**: Февраль 2026

Удачи! 🚀
